{
  "name": "crypto-proxy",
  "version": "2.0.0",
  "description": "高性能加密货币数据代理服务器",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",  // 升级到2.7.0
    "abort-controller": "^3.0.0",
    "https-proxy-agent": "^7.0.2",  // 升级到7.0.2
    "compression": "^1.7.4",  // 新增：响应压缩
    "helmet": "^7.0.0"  // 新增：安全头
  },
  "engines": {
    "node": ">=18.0.0"  // 新增：指定Node.js版本
  },
  "keywords": [
    "crypto",
    "proxy",
    "binance",
    "okx",
    "funding-rate"
  ],
  "license": "MIT"
}
